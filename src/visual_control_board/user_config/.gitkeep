# This directory is for user-specific configuration files (ui_config.yaml, actions_config.yaml).
# It is gitignored by default to prevent accidental commits of user data.
# Copy example configurations from `config_examples/` here to get started.
